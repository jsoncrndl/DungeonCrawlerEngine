#include "particle_system.h"

void Engine::Game::ECS::ParticleSystem::update(float deltaSeconds)
{
	// Loop through each emitter and update it
}

void Engine::Game::ECS::ParticleSystem::render(Graphics::Graphics* graphics)
{
	// Loop through each emitter and render its particles
}
