#pragma once

#include <steam/steamnetworkingtypes.h>

namespace Engine::Game::Network
{
	using NetworkConnection = HSteamNetConnection;
	/*class NetworkConnection
	{
		HSteamNetConnection m_connection;
		
	public:
		NetworkConnection();
		NetworkConnection(const HSteamNetConnection& connetion);
	};*/
}